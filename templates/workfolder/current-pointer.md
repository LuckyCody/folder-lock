# Current pointer — {slug}

**Phase 0 — scaffolded** ({date}, via new.py)

Next concrete action: read `{rel}/.goal/goal.md`, write the first executable step of the goal into this line, and start it.

After this: the remaining phases of the goal, one line each (fill in as they become clear).

Resume handle: read this file + `{rel}/.goal/goal.md` + `{rel}/memory.md`. Front door = this pointer until a CONTEXT.md exists.
