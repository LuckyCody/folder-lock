# Goal — {slug}

{goal}

complete: false

<!-- Flip `complete: false` to `complete: true` when the paragraph above is true. the signoff archives the folder
     (git mv -> _archive/{slug}/, registry entry marked archived) once complete: true AND the board holds zero
     open items for it. Nothing is deleted; memory.md is the reason this folder existed. -->
