# memory — {slug}

Why this folder exists: {goal}

Decisions (one dated line each; PROTOCOL §12 — a default an agent picked is logged here, never silently):

- {date}: folder created via new.py (`{rel}/`, registry id `{slug}`).
