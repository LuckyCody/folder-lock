# {slug} — progress log

Newest section on top. /signoff inserts one dated `## DONE — YYYY-MM-DD (<window>): <headline>` section per shift
(atomic rewrite, never the Edit tool — PROTOCOL §9 / signoff Step 2a).

## CREATED — {date}: scaffolded via new.py

Folder `{rel}/` created, registered (`{slug}` owns `{rel}/**`), claimed, put on the board. Goal: see `.goal/goal.md`.
